# Star Trek Galaxies -- paragon leader backgrounds.
#
# SHADOWS gfx/portraits/asset_selectors/paragon_backgrounds.txt. Vanilla 4.4
# verbatim -- its four legendary rows included -- plus 28 STG rows that reach
# STNH's leader-background art by species class and leader class.
#
# STNH's own copy of this file stays excluded in vendor.yml: every row in it is
# gated on a trigger decision 15 left inert or on a country flag STG never sets,
# so it would reach nothing while dropping vanilla's four.
#
# GENERATED by tools/gen_paragon_backgrounds.py -- edit that, not this.
# See .docs/decisions/48-paragon-backgrounds.md
paragon_background_selector = {
	default = "default_background"
	leader = {
		legendary_bg_azaryn = {
			has_leader_flag = azaryn
		}
		legendary_bg_beholder = {
			has_leader_flag = beholder
		}
		legendary_bg_skrand = {
			has_leader_flag = skrand
		}
		legendary_bg_keides = {
			has_leader_flag = exakeides
		}

		# -- Star Trek. Species class is the gate, as it is for clothing (decision 15).
		stg_fed_commander_bg = { leader_class = commander exists = owner owner = { owner_species = { is_species_class = FED } } }
		stg_fed_scientist_bg = { leader_class = scientist exists = owner owner = { owner_species = { is_species_class = FED } } }
		stg_fed_official_bg = { leader_class = official exists = owner owner = { owner_species = { is_species_class = FED } } }
		stg_kdf_commander_bg = { leader_class = commander exists = owner owner = { owner_species = { is_species_class = KDF } } }
		stg_kdf_scientist_bg = { leader_class = scientist exists = owner owner = { owner_species = { is_species_class = KDF } } }
		stg_kdf_official_bg = { leader_class = official exists = owner owner = { owner_species = { is_species_class = KDF } } }
		stg_rom_commander_bg = { leader_class = commander exists = owner owner = { owner_species = { is_species_class = ROM } } }
		stg_rom_scientist_bg = { leader_class = scientist exists = owner owner = { owner_species = { is_species_class = ROM } } }
		stg_rom_official_bg = { leader_class = official exists = owner owner = { owner_species = { is_species_class = ROM } } }
		stg_car_commander_bg = { leader_class = commander exists = owner owner = { owner_species = { is_species_class = CAR } } }
		stg_car_scientist_bg = { leader_class = scientist exists = owner owner = { owner_species = { is_species_class = CAR } } }
		stg_car_official_bg = { leader_class = official exists = owner owner = { owner_species = { is_species_class = CAR } } }
		stg_dom_commander_bg = { leader_class = commander exists = owner owner = { owner_species = { is_species_class = DOM } } }
		stg_dom_scientist_bg = { leader_class = scientist exists = owner owner = { owner_species = { is_species_class = DOM } } }
		stg_dom_official_bg = { leader_class = official exists = owner owner = { owner_species = { is_species_class = DOM } } }
		stg_fer_commander_bg = { leader_class = commander exists = owner owner = { owner_species = { is_species_class = FER } } }
		stg_fer_scientist_bg = { leader_class = scientist exists = owner owner = { owner_species = { is_species_class = FER } } }
		stg_fer_official_bg = { leader_class = official exists = owner owner = { owner_species = { is_species_class = FER } } }
		stg_brg_commander_bg = { leader_class = commander exists = owner owner = { owner_species = { is_species_class = BRG } } }
		stg_brg_scientist_bg = { leader_class = scientist exists = owner owner = { owner_species = { is_species_class = BRG } } }
		stg_brg_official_bg = { leader_class = official exists = owner owner = { owner_species = { is_species_class = BRG } } }
		stg_ter_official_bg = { leader_class = official exists = owner owner = { owner_species = { is_species_class = TER } } }
		stg_con_official_bg = { leader_class = official exists = owner owner = { owner_species = { is_species_class = CON } } }

		bg_admiral = {
			leader_class = commander
		}
		bg_scientist = {
			leader_class = scientist
		}
		bg_official = {
			leader_class = official
		}
		default_background = {
			always = yes
		}
	}
	ruler = {
		legendary_bg_azaryn = {
			has_leader_flag = azaryn
		}
		legendary_bg_beholder = {
			has_leader_flag = beholder
		}
		legendary_bg_skrand = {
			has_leader_flag = skrand
		}
		legendary_bg_keides = {
			has_leader_flag = exakeides
		}

		# -- Star Trek. Species class is the gate, as it is for clothing (decision 15).
		stg_fed_ruler_bg = { exists = owner owner = { owner_species = { is_species_class = FED } } }
		stg_fer_ruler_bg = { exists = owner owner = { owner_species = { is_species_class = FER } } }
		stg_brg_ruler_bg = { exists = owner owner = { owner_species = { is_species_class = BRG } } }
		stg_ter_ruler_bg = { exists = owner owner = { owner_species = { is_species_class = TER } } }
		stg_con_ruler_bg = { exists = owner owner = { owner_species = { is_species_class = CON } } }

		bg_ruler = {
			always = yes
		}
	}
}
