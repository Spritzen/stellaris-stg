# OVERRIDES interface/resource_groups/topbar_other_resource_groups.txt, which
# exists in BOTH vanilla 4.4 and the Universal Resource Patch. URP wins that path
# in the harvest (it is loaded last, per .docs/architecture/harvest-order.md) and this file wins over URP.
#
# URP lists 942 strategic-resource tokens, for every modded resource it knows
# about. Only 13 resolve in a standalone STG, and the engine logs each of the
# other 929 every time it parses the file — 929 records in the 2026-08-02 live
# run. Nothing is lost by filtering: all 13 working entries are below, and every
# token removed named a resource this build does not define.
#
# PHASE 4: when STG defines its own Trek resources (sr_latinum, sr_dilithium,
# sr_ketracel_white, sr_crew — the block URP annotates "688086068 ST: New
# Horizons") add them to RARE_RESOURCES here. They are why .docs/architecture/harvest-order.md wanted
# URP's file.
#
# Generated by filtering URP's file to tokens that resolve against
# common/strategic_resources/ in the merged tree; category structure is URP's.

tb_others_group = {
	category = {
		name = "RARE_RESOURCES"
		resources = {
			volatile_motes
			exotic_gases
			rare_crystals
			sr_living_metal
			sr_zro
			sr_dark_matter
			nanites
			sr_eludium
			sr_acean
			biomass
		}
		onclick = market
	}

	category = {
		name = "EXCEPTIONAL_MATERIALS"
		resources = {
			minor_artifacts
			astral_threads
			entropy_crystals
		}
		onclick = discoveries
		show_only_if_owned = yes
		additional_element_height = 25
	}

	localization = {
		"RESOURCE_GROUP_DEFAULT" = default
		"RESOURCE_GROUP_DEFAULT_NEG_DEC_TOTAL_POS" = { total_balance >= 0 balance < 0 stored >= 1000 stored < 10000 }
		"RESOURCE_GROUP_DEFAULT_NEG_DEC" = { balance < 0 stored >= 1000 stored < 10000 }
		"RESOURCE_GROUP_DEFAULT_NEG_TOTAL_POS" = { total_balance >= 0 balance < 0 }
		"RESOURCE_GROUP_DEFAULT_NEG" = { balance < 0 }
		"RESOURCE_GROUP_DEFAULT_MAX_DEC" = { max > 0 stored >= max total_stored >= 1000 total_stored < 10000 }
		"RESOURCE_GROUP_DEFAULT_MAX" = { max > 0 stored >= max }
		"RESOURCE_GROUP_DEFAULT_DEC" = { stored >= 1000 stored < 10000 }
	}

	desc = "concept_strategic_resources_tooltip"
	minimum_height = 175
}
