# Star Trek Galaxies — triggers the prescripted empires gate on.
#
# `playable` on a prescripted country takes a scripted trigger NAME, never an
# inline block — vanilla only ever writes `playable = has_shroud_dlc`. So an
# AI-only empire needs a trigger that is always false, and this is it.
# Used by every empire in src/prescripted_countries/stg_minor_powers.txt.

stg_never = {
	always = no
}
