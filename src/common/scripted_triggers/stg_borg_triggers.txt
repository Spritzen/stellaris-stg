# is_borg_empire — a real body for the one STNH trigger that gates STG content.
# Key unprefixed because vendored STNH art references it verbatim; see
# .docs/decisions/10-species-class-keys-unprefixed.md.
#
# stg_stnh_art_triggers.txt stubs STNH's triggers to `always = no`; this one
# earns a body because it is the only `add = 100` in the l_vo_borg advisor
# voice weight, and STG ships a Borg empire the user plays. Decision 12.
#
# Tests the species class rather than a country flag: the voice is picked in
# the empire creator, before any event could set one. BRG is `randomized = no`
# and stg_borg_collective is the only thing that names it. Scope is country,
# as the advisor voice weight block requires.
#
# Two siblings complete the repair — the `vo_borg` display name in
# src/localisation/english/stg_advisor_voices_l_english.yml, and
# src/gfx/interface/icons/traits/trait_assimilated.dds, which is a copy of
# vanilla's trait_cybernetic.dds (a .dds carries no header comment, so its
# provenance is recorded here).

is_borg_empire = {
	owner_species = { is_species_class = BRG }
}
