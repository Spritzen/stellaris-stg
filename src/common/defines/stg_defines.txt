# STG define overrides.
#
# ── CUSTOM_EMPIRE_SPAWN_CHANCE ─────────────────────────────────────────────
# Vanilla ships 50 on a `10 = 1%` scale (00_defines.txt:972): a 5% chance that
# any given AI slot is filled from the prescripted pool rather than randomly
# generated. 1000 is 100%, which is safe here only because decision 14 left the
# prescripted pool all-Trek and decision 19 made it deep enough to fill a
# galaxy.
#
# THIS IS NOT A CLOSED QUESTION, whatever decision 86 says. Two galaxies have
# now been generated at 1000, the second with all 99 designs loaded, and both
# contained zero Trek AI empires. The define was a real defect and is a real
# fix; it was never the cause. Do not read this file as the answer to "why is
# there no Trek in the galaxy", and do not raise or lower the value looking for
# one -- at 100% the draw rate is no longer the variable.
# .docs/planning/open-questions.md, "Whether the galaxy is Trek now"
# .docs/decisions/86-prescripted-empires-never-drawn.md
# .docs/decisions/88-playable-gates-the-design-database.md
#
# This is the only lever of its kind: the designer's force-spawn checkbox exists
# for player-made empires and a prescripted one cannot reach it.
# FALLEN_CUSTOM_EMPIRE_SPAWN_CHANCE stays at vanilla's 50 -- STG ships no
# prescripted fallen empire for it to find.
NGameplay = {
	CUSTOM_EMPIRE_SPAWN_CHANCE = 1000
}
