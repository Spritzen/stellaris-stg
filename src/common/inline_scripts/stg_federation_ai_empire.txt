# GENERATED by tools/gen_home_systems.py -- do not hand-edit; regenerate.
#
# The AI copy of stg_united_federation_of_planets, as an inline_script fragment.
#
# This empire's home system belongs to a vendored source rather than to us, so
# the guarded create_country below has nowhere in src/ to live. vendor.yml
# splices this file into that initializer's capital with one
# `inline_script = "stg_federation_ai_empire"` line, and the engine includes it
# verbatim -- the same include vanilla uses nine times in
# common/solar_system_initializers/.
#
# The block is the SAME ai_empire_block the other 36 home systems carry, read
# from this empire's own prescripted_countries/ entry, so it cannot drift from
# the empire it copies. Everything the guard does and why is documented on
# ai_empire_block in the generator.
#
# See .docs/decisions/107-the-ai-federation.md.

init_effect = {
	# The AI copy of stg_united_federation_of_planets, created only when no country is
	# already playing it. See ai_empire_block in the generator.
	if = {
		limit = { NOT = { any_country = { has_country_flag = stg_united_federation_of_planets } } }
		create_species = {
			name = "STG_species_name_federation"
			plural = "STG_species_plural_federation"
			adjective = "STG_species_adjective_federation"
			class = FED
			portrait = "human"
			homeworld = THIS
			namelist = "STG_FEDERATION"
			traits = {
				trait = "trait_organic"
				trait = "trait_adaptive"
				trait = "trait_nomadic"
				trait = "trait_wasteful"
				ideal_planet_class = "pc_continental"
			}
			effect = { save_event_target_as = united_federation_of_planets_species }
		}
		create_country = {
			name = "STG_EMPIRE_federation"
			adjective = "STG_adjective_federation"
			type = default
			authority = "auth_democratic"
			civics = { civic = "civic_beacon_of_liberty" civic = "civic_diplomatic_corps" }
			ethos = { ethic = "ethic_fanatic_xenophile" ethic = "ethic_egalitarian" }
			origin = "origin_default"
			species = event_target:united_federation_of_planets_species
			name_list = "STG_FEDERATION"
			ship_prefix = "STG_ship_prefix_federation"
			flag = {
				icon = { category = "trek" file = "Federation1.dds" }
				background = { category = "backgrounds" file = "00_solid.dds" }
				colors = { "blue" "light_blue" "null" "null" }
			}
			effect = {
				save_event_target_as = united_federation_of_planets_country
				set_graphical_culture = starfleet_tng
				set_city_graphical_culture = humanoid_01
				set_country_flag = stg_united_federation_of_planets
			}
		}
		create_colony = { owner = event_target:united_federation_of_planets_country }
		generate_start_deposits_and_blockers = yes
		clear_blockers = yes
		colony = {
			generate_start_buildings_and_districts = yes
			generate_start_pops = yes
		}
		event_target:united_federation_of_planets_country = {
			create_starting_leaders = yes
			country_event = { id = game_start.9 }
			country_event = { id = game_start.33 days = 1 }
		}
	}
}
