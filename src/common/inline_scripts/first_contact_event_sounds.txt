# first_contact_event_sounds -- OVERRIDES vanilla's file of the same
# path, which is the only way to reach it: the inline script is spliced
# into vanilla's own first-contact events and we shadow none of those.
#
# WHY. Vanilla's list gates every sound on `is_species_class` against its
# own thirteen classes. STG declares 129 of its own and matches none, so
# no block passes and the engine falls back to the first entry on the
# list -- `event.cpp:896`, ten times in the 2026-08-28 UFP run, once per
# first contact, every one of them playing the AQUATIC sting.
#
# Vanilla's thirteen blocks are carried through unchanged and come first:
# this file replaces vanilla's, so a block not copied here is dropped for
# vanilla species too (decision 07's failure mode).
#
# GENERATED by tools/gen_first_contact_sounds.py -- do not hand-edit. The
# mapping is total by construction: all 129 classes, each in exactly
# one block, and the tool dies rather than emit a partial one. `make
# gen-check` asks every run whether this is still what the inputs produce.
# .docs/decisions/101-first-contact-sounds-are-species-class-gated.md

# ── vanilla's thirteen ────────────────────────────────────────────────
	show_sound = {
		sound = event_first_contact_aquatic
		trigger = { contact_country? = { is_species_class = AQUATIC } }
	}
	show_sound = {
		sound = event_first_contact_arthropoid
		trigger = { contact_country? = { is_species_class = ART } }
	}
	show_sound = {
		sound = event_first_contact_avian
		trigger = { contact_country? = { is_species_class = AVI } }
	}
	show_sound = {
		sound = event_first_contact_fungoid
		trigger = { contact_country? = { is_species_class = FUN } }
	}
	show_sound = {
		sound = event_first_contact_humanoid
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = HUM
					is_species_class = MSI_SLAVER
					is_species_class = MINDWARDEN
					is_species_class = MINDWARDEN_ENCLAVE
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_lithoid
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = LITHOID
					is_species_class = SHROUDWALKER
					is_species_class = INF
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_machine
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = MACHINE
					is_species_class = ROBOT
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_mammalian
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = MAM
					is_species_class = SALVAGER
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_molluscoid
		trigger = { contact_country? = { is_species_class = MOL } }
	}
	show_sound = {
		sound = event_first_contact_necroid
		trigger = { contact_country? = { is_species_class = NECROID } }
	}
	show_sound = {
		sound = event_first_contact_plantoid
		trigger = { contact_country? = { is_species_class = PLANT } }
	}
	show_sound = {
		sound = event_first_contact_reptilian
		trigger = { contact_country? = { is_species_class = REP } }
	}
	show_sound = {
		sound = event_first_contact_toxoid
		trigger = { contact_country? = { is_species_class = TOX } }
	}

# ── STG's, by the nearest reading of the species ──────────────────────
	show_sound = {
		sound = event_first_contact_machine
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = BRG 	# Borg -- drones, and the class is machine-intelligence only
					is_species_class = HOLO	# photonic lifeforms; the class is built on vanilla's ROBOT
					is_species_class = CRA 	# Cravic -- Automated Personnel Units, robots outright
					is_species_class = PRA 	# Pralor -- the other half of the same war, same robots
					is_species_class = BYN 	# Bynars, bonded to their computer from birth
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_lithoid
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = THO 	# Tholians -- crystalline, and the class carries LITHOID
					is_species_class = BRIK	# Brikar -- rock-bodied
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_necroid
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = KOB	# Kobali, who reproduce by reanimating other species' dead
					is_species_class = VID	# Vidiians -- the Phage, and organ harvesting
					is_species_class = MED	# Medusans -- non-corporeal, and unbearable to look at
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_toxoid
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = MAL	# Malon -- theta-radiation haulers, toxic by trade
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_reptilian
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = CAR	# Cardassians
					is_species_class = GOR	# Gorn
					is_species_class = TRO	# T'Rogorans, Gorn-adjacent
					is_species_class = SEL	# Selay -- serpentine
					is_species_class = SAU	# Saurians
					is_species_class = VOT	# Voth -- descended from Earth's hadrosaurs
					is_species_class = XIN	# Xindi -- six sub-species, and the Reptilians are the face of them
					is_species_class = HAZ	# Hazari -- the species class file calls them tuatara
					is_species_class = TZE	# Tzenkethi
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_arthropoid
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = UND	# Undine -- Species 8472, tripedal and not remotely humanoid
					is_species_class = HUR	# Hur'Q -- insectoid
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_mammalian
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = CAI 	# Caitians -- felinoid
					is_species_class = KZI 	# Kzinti -- felinoid
					is_species_class = LYR 	# Lyrans -- felinoid
					is_species_class = LYRI	# Lyridians -- the diaspora of the same
					is_species_class = ANTI	# Anticans -- canine
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_aquatic
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = ANT	# Antedeans -- piscine
					is_species_class = MON	# Moneans, who live in a water world and nothing else
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_molluscoid
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = DOM	# Founders -- liquid-state, and BIOLOGICAL is only vanilla's nearest archetype
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_plantoid
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = SHE	# Sheliak -- chlorophyll-based, and contemptuous of carbon units
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_avian
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = KIN	# Kinshaya -- winged, and the only wings in the roster
				}
			}
		}
	}
	show_sound = {
		sound = event_first_contact_humanoid
		trigger = {
			contact_country? = {
				OR = {
					is_species_class = FED
					is_species_class = VUL
					is_species_class = KDF
					is_species_class = ROM
					is_species_class = FER
					is_species_class = BAJ
					is_species_class = TRI
					is_species_class = ADR
					is_species_class = BOL
					is_species_class = BRE
					is_species_class = SUL
					is_species_class = YRI
					is_species_class = KRE
					is_species_class = TER
					is_species_class = ACA
					is_species_class = ARG
					is_species_class = BEN
					is_species_class = BET
					is_species_class = BRU
					is_species_class = CHE
					is_species_class = CHER
					is_species_class = CON
					is_species_class = DEL
					is_species_class = DEN
					is_species_class = DEV
					is_species_class = DOS
					is_species_class = ELA
					is_species_class = FEN
					is_species_class = FLA
					is_species_class = GAR
					is_species_class = HAA
					is_species_class = HIR
					is_species_class = HUS
					is_species_class = HYDR
					is_species_class = KAR
					is_species_class = KAZ
					is_species_class = KES
					is_species_class = KRA
					is_species_class = KRI
					is_species_class = LIS
					is_species_class = LUR
					is_species_class = MOR
					is_species_class = NAU
					is_species_class = NOR
					is_species_class = NYB
					is_species_class = NYG
					is_species_class = ORI
					is_species_class = OSC
					is_species_class = PAK
					is_species_class = PAR
					is_species_class = RAK
					is_species_class = RIG
					is_species_class = SKR
					is_species_class = TAL
					is_species_class = TALA
					is_species_class = TAM
					is_species_class = TEL
					is_species_class = TRA
					is_species_class = TUR
					is_species_class = UNI
					is_species_class = VAA
					is_species_class = VAL
					is_species_class = VALT
					is_species_class = VAU
					is_species_class = WAD
					is_species_class = XEP
					is_species_class = YAD
					is_species_class = ZAH
					is_species_class = ZAK
					is_species_class = AEN
					is_species_class = ALTAIR
					is_species_class = ALY
					is_species_class = BAR
					is_species_class = BOM
					is_species_class = BREK
					is_species_class = CAL
					is_species_class = CAPELLA
					is_species_class = DORVAN
					is_species_class = EDO
					is_species_class = EDOS
					is_species_class = EVO
					is_species_class = HOT
					is_species_class = HUP
					is_species_class = ILL
					is_species_class = KEL
					is_species_class = KOBL
					is_species_class = KREE
					is_species_class = KRESS
					is_species_class = MIN
					is_species_class = MIR
					is_species_class = MIZ
					is_species_class = ORN
					is_species_class = REN
					is_species_class = TARK
					is_species_class = TLS
					is_species_class = XAH
					is_species_class = XYR
				}
			}
		}
	}
